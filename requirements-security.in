# Semgrep is intentionally isolated from the MCP feasibility environment:
# semgrep==1.175.0 has a hard dependency on mcp==1.29.0, while this project
# pins the official MCP feasibility SDK at mcp==2.1.1.  Combining them would
# make the development lock unsatisfiable and would weaken the SDK decision.
semgrep==1.175.0
