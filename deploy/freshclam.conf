# The updater is the sole component with its own restricted egress network.
DatabaseDirectory /var/lib/clamav
DatabaseMirror database.clamav.net
Checks 1
Foreground true
UpdateLogFile /tmp/freshclam.log
